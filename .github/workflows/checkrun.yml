name: test

on:
  check_run:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2
      - env:
          MESSAGE: ${{ github.event.output.title }}
        run: echo $MESSAGE


